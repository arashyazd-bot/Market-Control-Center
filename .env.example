# Free FRED API key — get one instantly at https://fredaccount.stlouisfed.org/apikeys
# The dashboard runs without it (on bundled sample data), but live macro/rates
# data requires this key.
FRED_API_KEY=
